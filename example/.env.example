# Copy to `example/.env` and fill in. Never commit real keys.
# EXPO_PUBLIC_ vars are inlined into the app bundle — use a scoped, restricted key.
EXPO_PUBLIC_ARCGIS_API_KEY=your-scoped-arcgis-api-key
