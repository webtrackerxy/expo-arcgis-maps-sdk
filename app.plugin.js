// Entry point Expo uses to resolve this package's config plugin. It points at
// the compiled output so consumers can add "expo-arcgis-maps-sdk" to their
// app config `plugins` array. Run `yarn build:plugin` to (re)generate ./plugin/build.
module.exports = require('./plugin/build');
